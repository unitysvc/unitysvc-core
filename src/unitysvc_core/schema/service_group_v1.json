{
  "$defs": {
    "GroupOwnerTypeEnum": {
      "description": "Type of entity that owns a service group.",
      "enum": [
        "platform",
        "seller",
        "customer"
      ],
      "title": "GroupOwnerTypeEnum",
      "type": "string"
    },
    "GroupTypeEnum": {
      "description": "Type of service group. Derived from members, not authored.\n\n- ``category``: Parent group for browsing/organization only.\n- ``collection``: Services-bearing group that is not routable.\n- ``open``: Routable group where keyless requests can fan safely.\n- ``keyed``: Routable group where callers must provide a routing key.",
      "enum": [
        "category",
        "collection",
        "open",
        "keyed"
      ],
      "title": "GroupTypeEnum",
      "type": "string"
    },
    "ServiceGroupStatusEnum": {
      "enum": [
        "draft",
        "active",
        "private",
        "archived"
      ],
      "title": "ServiceGroupStatusEnum",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Strict file-authoring variant of :class:`ServiceGroupData`.\n\nIdentical fields to ``ServiceGroupData`` but with ``extra=\"forbid\"`` so the\nCLI's file validation rejects typo'd keys. Its JSON Schema\n(``additionalProperties: false``) is what ``usvc_seller specs validate``\nchecks ``service_group.{json,toml}`` files against.\n\nExample file::\n\n    {\n        \"name\": \"my-llm-services\",\n        \"display_name\": \"My LLM Services\",\n        \"description\": \"LLM services for targeted promotions\",\n        \"membership_rules\": {\"expression\": \"service_type == 'llm'\"},\n        \"status\": \"private\"\n    }",
  "properties": {
    "access_interface_data_template": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Jinja2 template that renders to AccessInterfaceData JSON when populated with service data. Used by admin to auto-generate group-scoped AccessInterfaces.",
      "title": "Access Interface Data Template"
    },
    "description": {
      "anyOf": [
        {
          "maxLength": 2000,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Detailed description of the group",
      "title": "Description"
    },
    "display_name": {
      "description": "Human-readable name for UI display",
      "maxLength": 200,
      "title": "Display Name",
      "type": "string"
    },
    "group_type": {
      "$ref": "#/$defs/GroupTypeEnum",
      "default": "category",
      "description": "Backend-derived group type: category (browse-only parent), collection (not routable), open (routable keyless fanout), or keyed (routable by key)."
    },
    "membership_rules": {
      "anyOf": [
        {
          "additionalProperties": true,
          "type": "object"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Rules for automatic service membership. Format: {\"expression\": \"<python_expression>\"}\nAvailable variables: service_id, seller_id, provider_id, seller_name, provider_name, name, display_name, service_type, status, listing_type, tags, is_featured",
      "title": "Membership Rules"
    },
    "name": {
      "description": "URL-friendly slug, unique per owner (e.g., 'my-llm-services'). Lowercase ASCII alphanumeric plus '-' / '_', must start with a letter or digit. Used as the public identifier in customer SDK paths, so changing it is a breaking change for callers.",
      "maxLength": 100,
      "minLength": 1,
      "pattern": "^[a-z0-9][a-z0-9_-]*$",
      "title": "Name",
      "type": "string"
    },
    "owner_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Owner ID (UUID string). For platform groups, the backend fills in the platform-superuser ID when omitted; for seller/customer groups it must be supplied.",
      "title": "Owner Id"
    },
    "owner_type": {
      "$ref": "#/$defs/GroupOwnerTypeEnum",
      "default": "platform",
      "description": "Type of owner (platform, seller, customer)"
    },
    "parent_group_name": {
      "anyOf": [
        {
          "maxLength": 100,
          "minLength": 1,
          "pattern": "^[a-z0-9][a-z0-9_-]*$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Parent group name for hierarchy (resolved to ancestor_path by the backend). Must itself be a valid group slug.",
      "title": "Parent Group Name"
    },
    "sort_order": {
      "default": 0,
      "description": "Display order within parent level",
      "title": "Sort Order",
      "type": "integer"
    },
    "status": {
      "$ref": "#/$defs/ServiceGroupStatusEnum",
      "default": "draft",
      "description": "Group status (draft, active, private, archived)"
    }
  },
  "required": [
    "name",
    "display_name"
  ],
  "title": "ServiceGroupV1",
  "type": "object"
}
